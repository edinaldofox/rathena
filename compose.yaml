services:
  rathena:
    build:
      context: .
    container_name: rathena-server
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ragnarok
      DB_USER: ragnarok
      DB_PASSWORD: ragnarok
    ports:
      - "6900:6900"
      - "6121:6121"
      - "5121:5121"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pgrep -f './login-server' >/dev/null && pgrep -f './char-server' >/dev/null && pgrep -f './map-server' >/dev/null && ss -ltn '( sport = :6900 or sport = :6121 or sport = :5121 )' | grep -q ':6900' && ss -ltn '( sport = :6900 or sport = :6121 or sport = :5121 )' | grep -q ':6121' && ss -ltn '( sport = :6900 or sport = :6121 or sport = :5121 )' | grep -q ':5121'"
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    restart: unless-stopped
  db:
    image: mariadb:10.11
    container_name: rathena-db
    environment:
      MARIADB_ROOT_PASSWORD: ragnarok
      MARIADB_DATABASE: ragnarok
      MARIADB_USER: ragnarok
      MARIADB_PASSWORD: ragnarok
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./sql-files/main.sql:/docker-entrypoint-initdb.d/01-main.sql:ro
      - ./sql-files/logs.sql:/docker-entrypoint-initdb.d/02-logs.sql:ro
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "127.0.0.1", "-uroot", "-pragnarok", "--silent"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

volumes:
  mariadb_data:
